package Alishev._Модификаторы_доступа;
public class Test {
    /*
    Класс может быть public или default. Но в каждом JAVA файле должен быть хотя бы
    один публичный класс.
     */
    public static void main(String[] args) {
        /* Модификаторы доступа - public, private, default, protected
        public - позволяет доступ внутри проекта.
        private - доступ только в пределах одного класса.
        default - "по-умолчанию", доступно в пределах одного пакета.
        protected - доступ внутри пакета, а так же тому кто наследует этот класс.
         */

    }
}
